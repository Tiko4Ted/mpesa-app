name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.4'
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install npm dependencies
        run: npm ci

      - name: Validate Expo dependencies
        run: npx expo install --check

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Bundle Android JS
        run: npx expo export --platform android --output-dir dist-android

      - name: Run Expo Prebuild
        run: npx expo prebuild --platform android --clean --no-install

      - name: Make Gradle wrapper executable
        run: chmod +x android/gradlew

      - name: Build APK (Assemble Release)
        working-directory: android
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mpesa-app-release
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 7

      - name: Prepare release APK
        run: cp android/app/build/outputs/apk/release/app-release.apk mpesa.apk

      - name: Publish latest APK release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest-apk
          name: Latest Android APK
          body: "Latest Android APK built from main."
          files: mpesa.apk
          make_latest: true
